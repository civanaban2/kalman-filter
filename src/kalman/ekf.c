#include "kalman.h"

void	ekf(kalman_t *kalman, measurement_t new_data, measurement_t last_data)
{
	(void)last_data; // last_data is not used in this function
	(void)new_data; // new_data is not used in this function
	(void)kalman; // kalman is not used in this function
}